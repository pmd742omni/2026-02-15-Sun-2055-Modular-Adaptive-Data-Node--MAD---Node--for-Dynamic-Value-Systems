## 4.1 System Architecture

The **Modular Adaptive Data Node (MADN)** architecture is structured as a decoupled, multi-tiered edge computing appliance optimized for resource-constrained, off-grid deployments.

```mermaid
flowchart TD
    subgraph Client_Tier ["Client & Interface Tier (Zero-Install HTML5 / Glassmorphic UI)"]
        UI_POS["🪙 POS Register & Split-Tender Checkout"]
        UI_AGRI["🌾 Crop Lifecycle & Production Cost Calculator"]
        UI_SEC["🛡️ Visitor Gatekeeper & Access Control"]
        UI_SOC["🌐 4-in-1 Hyperlocal Social Hub"]
        UI_RECEIPT["🧾 Thermal & PDF Receipt Viewer + 2D QR Code"]
        UI_ADMIN["👥 Multi-Tenant Staff & Permissions Manager"]
    end

    subgraph API_Tier ["FastAPI Asynchronous Gateway & Hierarchical RBAC"]
        AUTH_GUARD["🔐 Hierarchical RBAC & Session Token Validator"]
        ROUTE_BIZ["🏢 Multi-Tenant Scoping Controller"]
        ROUTE_VOUCH["🎟️ HMAC-SHA256 Bearer Voucher Engine"]
        ROUTE_POS["💳 Mixed-Tender Settlement Router"]
        ROUTE_AGRI["🌱 Lifecycle & Spoilage Optimization Router"]
        ROUTE_AUDIT["🔒 Forensic Hash Chain Logger"]
    end

    subgraph Data_Tier ["SQLite 3.45 Write-Ahead Logging (WAL) Tri-Ledger"]
        DB_USERS["users (Auth & Global Roles)"]
        DB_BIZ["businesses (Enterprise Profiles)"]
        DB_OPS["business_operators (Granular Subsystem Permissions)"]
        DB_INV["inventory (Perishable Batches & Decay Prices)"]
        DB_AGRI["agri_plantings, agri_production_costs, agri_harvests"]
        DB_TX["transactions, tx_items, tx_tenders"]
        DB_VOUCH["vouchers (Cryptographic Bearer Tokens)"]
        DB_SEC["security_visitor_logs (Tamper-Evident Logs)"]
        DB_SOC["social_posts (4-Paradigm Hyperlocal Content)"]
        DB_AUDIT["audit_log (Cryptographic HMAC Hash Chain)"]
    end

    Client_Tier --> AUTH_GUARD
    AUTH_GUARD --> ROUTE_BIZ
    AUTH_GUARD --> ROUTE_VOUCH
    AUTH_GUARD --> ROUTE_POS
    AUTH_GUARD --> ROUTE_AGRI
    AUTH_GUARD --> ROUTE_AUDIT

    ROUTE_BIZ --> DB_BIZ
    ROUTE_BIZ --> DB_OPS
    ROUTE_VOUCH --> DB_VOUCH
    ROUTE_POS --> DB_INV
    ROUTE_POS --> DB_TX
    ROUTE_AGRI --> DB_AGRI
    ROUTE_AUDIT --> DB_AUDIT
```

---

### 4.1.1 Hierarchical Multi-Tenant RBAC Model

The authorization subsystem implements a three-tier delegation architecture:

1. **Tier 1 — System Root / Platform Administrator**:
   - Manages physical hardware telemetry, 802.11s RF mesh clusters, captive portal network boundaries, and registers new commercial enterprise entities.

2. **Tier 2 — Business Administrator (Enterprise Owner)**:
   - Owns and manages a specific business partition (`business_id`).
   - Delegates access by inviting staff members and provisioning fine-grained subsystem permissions:
     $$\mathcal{P}_{\text{operator}} \subseteq \{\text{pos}, \text{inventory}, \text{agriculture}, \text{security}, \text{social}, \text{vouchers}, \text{reports}\}$$

3. **Tier 3 — Delegated Business Operators (Staff / Employees)**:
   - Access is restricted exclusively to granted subsystem databases.
   - Cross-enterprise queries are rejected at the database level by parameterized `WHERE business_id = ?` filters.
