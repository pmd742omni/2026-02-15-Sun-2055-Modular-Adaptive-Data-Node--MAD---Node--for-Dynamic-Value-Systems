## 4.1 System Architecture

The **Modular Adaptive Data Node (MADN)** architecture implements a tiered, resilient computing model engineered for off-grid peri-urban deployments. The design eliminates centralized cloud server dependencies through an autonomous local-first architecture supporting multi-business tenancy, offline bearer tokens, dynamic inventory pricing, and cryptographic audit chains.

```mermaid
graph TB
    subgraph Multi_Tenant_Ecosystem ["Multi-Business Tenant Ecosystem"]
        B1["Tenant 1: Green Valley Organic Horticulture"]
        B2["Tenant 2: Khumalo Milling & Grains Co."]
        B3["Tenant 3: Matopos Heritage Dairy & Livestock"]
    end

    subgraph Presentation_Tier ["Presentation & Edge Client Tier"]
        UI_SPA["VisionPro Glassmorphic SPA (HTML5 / CSS3 / ES6)"]
        UI_POS["POS Terminal & Multi-Currency Split Register"]
        UI_RECEIPT["Thermal 58mm/80mm ESC/POS & Vector PDF Engine"]
        UI_QR["Vector 2D QR Matrix Synthesis Canvas"]
    end

    subgraph Service_Tier ["Micro-Service & Application Gateway Tier (FastAPI)"]
        GW_AUTH["Dynamic RBAC & scrypt / RFC 6238 TOTP Gatekeeper"]
        GW_TENANT["Multi-Tenant Isolation & Scoping Controller"]
        GW_DECAY["Continuous Exponential Decay Engine"]
        GW_VOUCHER["Offline Cryptographic Voucher Engine (HMAC-SHA256)"]
        GW_DISCOVERY["UDP Multicast Discovery Daemon (224.0.0.251:8001)"]
    end

    subgraph Persistence_Tier ["Persistence & Cryptographic Ledger Tier (SQLite WAL)"]
        TBL_BIZ[("businesses")]
        TBL_VOUCH[("vouchers")]
        TBL_INV[("inventory & inventory_wastage")]
        TBL_AGRI[("agri_plantings, agri_harvests & agri_production_costs")]
        TBL_TX[("transactions, transaction_items & transaction_tenders")]
        TBL_AUDIT[("audit_logs & shift_handovers (Chained HMAC-SHA256)")]
    end

    Multi_Tenant_Ecosystem --> UI_SPA
    UI_SPA --> GW_AUTH
    UI_POS --> GW_TENANT
    GW_TENANT --> GW_DECAY
    GW_TENANT --> GW_VOUCHER
    GW_VOUCHER --> UI_QR
    GW_VOUCHER --> UI_RECEIPT
    GW_AUTH --> Persistence_Tier
    GW_TENANT --> Persistence_Tier
    GW_VOUCHER --> Persistence_Tier
```

### 4.1.1 Key Architectural Highlights
1. **Multi-Tenant Enterprise Scoping**: Complete logical isolation of inventory, costs, plantings, sales, branded thermal slips, and customer vouchers across multiple independent businesses running concurrently on a single low-power edge node.
2. **Zero-Internet Bearer Token Liquidity**: The offline voucher engine mints and validates cryptographic bearer tokens in $<0.1\text{ms}$, solving rural small-change shortages without relying on third-party payment gateways or national telecommunications infrastructure.
3. **Decentralized UDP Multicast Clustering**: Autonomous peer discovery on port `8001` allows Data Nodes, Vault Nodes, and Operator Terminals to auto-cluster dynamically.
4. **Append-Only Cryptographic Audit Anchors**: Dual-ledger verification linking relational database log entries with append-only flat text files via chained HMAC-SHA256 math.
